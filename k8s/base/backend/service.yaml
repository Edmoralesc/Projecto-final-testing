apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app.kubernetes.io/name: fastticket-backend
    app.kubernetes.io/part-of: fastticket
spec:
  type: ClusterIP # Internal-only to avoid public LoadBalancer cost
  selector:
    app.kubernetes.io/name: fastticket-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8000
