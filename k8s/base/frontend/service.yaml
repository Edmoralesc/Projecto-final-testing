apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app.kubernetes.io/name: fastticket-frontend
    app.kubernetes.io/part-of: fastticket
spec:
  type: ClusterIP # Use Ingress or port-forwarding during dev to avoid ELB cost
  selector:
    app.kubernetes.io/name: fastticket-frontend
  ports:
    - name: http
      port: 3000
      targetPort: 80
